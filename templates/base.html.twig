<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Village Numérique Résistant{% endblock %}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;600;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="{{ asset('css/style.css') }}">
    {% block stylesheets %}{% endblock %}
</head>
<body data-page="{% block page_id %}{% endblock %}">
<nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container">
        <a class="navbar-brand fw-bold" href="{{ path('home') }}">⚔️ Village Résistant NIRD</a>

        {# 👇 AJOUTER ce bouton #}
        <button class="navbar-toggler" type="button"
                data-bs-toggle="collapse" data-bs-target="#menu"
                aria-controls="menu" aria-expanded="false" aria-label="Basculer la navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div id="menu" class="collapse navbar-collapse">
            <ul class="navbar-nav ms-auto">
                <li><a class="nav-link" href="{{ path('home') }}">Accueil</a></li>
                <li><a class="nav-link" href="{{ path('challenges') }}">Défis</a></li>
                <li><a class="nav-link" href="{{ path('quiz_play') }}">Quiz</a></li>
                <li><a class="nav-link" href="{{ path('resource_list') }}">Ressources</a></li>
            </ul>
            <div class="d-flex ms-lg-3">
                <button id="themeToggle" class="btn btn-theme tt" data-bs-toggle="tooltip"
                        title="Basculer le thème" aria-pressed="false" aria-label="Basculer le thème">🌓</button>
            </div>
        </div>
    </div>
</nav>


<div class="container mt-4">
    {% block body %}{% endblock %}
</div>

<footer class="mt-5">
    <p class="m-0">Nuit de l’Info 2025 — Village Numérique Résistant</p>
</footer>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
<script type="module" src="{{ asset('js/app.js') }}"></script>
{% block javascripts %}{% endblock %}
</body>
</html>
